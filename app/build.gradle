plugins {
    id 'com.android.application'
}

android {
    namespace 'com.homeflow.test'
    compileSdk 35

    defaultConfig {
        applicationId 'com.homeflow.stable'
        minSdk 26
        targetSdk 35
        versionCode 11
        versionName '1.1-review-fixes'
    }

    buildTypes {
        debug { minifyEnabled false }
        release { minifyEnabled false }
    }
}
